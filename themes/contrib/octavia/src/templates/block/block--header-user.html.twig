{% extends 'block--system-menu-block.html.twig' %}
{# Header user blocks display in columns. #}
{% set classes = classes ?? [] %}
{%
  set classes = classes|merge([
    'column',
    'is-narrow',
  ])
%}